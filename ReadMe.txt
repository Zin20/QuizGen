How to run:
I use node for the backend so you will need to have npm installed. Once it is installed run npm start and a backend server will start on localhost:3000.
In your browser go to localhost:3000 to hit the backend endpoint.